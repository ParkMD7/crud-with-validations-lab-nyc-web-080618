<h1>All Songs</h1>
<tbody>
  <% @songs.each do |song| %>
  <tr>
    <p>
      <%= link_to(song.title, song_path(song)) %>
    </p>
  </tr>
  <p>
    <%= song.artist_name %>
  </p>
  <p>
    <%= song.release_year %>
  </p>
  <p>
    <%= song.genre %>
  </p>
  <% end %>
</tbody>
